.left.white
    .center-center
        .copy
            h1 Connecting the device to your wiﬁ network.
            p Check the lists of available access points then click on your local network. The page will change once selected.
.right
    .center-center.panel(style="top: 25%").choose-wifi
        .wifi-content(ng-hide="wifis")
            h3 FINDING NETWORKS
            .spinner
        .wifi-content(ng-show="wifis")
            h3 SELECT FROM THIS LIST
            ul.wifis
                li(ng-repeat="wifi in wifis" ng-if="wifi.ssid.length > 0" ng-click="wifiSelected(wifi)") {{ wifi.ssid }}
